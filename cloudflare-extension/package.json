{
  "name": "@tjosepo/azure-pipelines",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "package": "tfx extension create --manifest-globs vss-extension.json",
    "publish": "tfx extension publish --manifest-globs vss-extension.json --share-with tommyjosepovic0409"
  },
  "dependencies": {
    "tfx-cli": "^0.14.0"
  }
}